apiVersion: v1
kind: Service
metadata:
  name: {{ template "qycloud.fullname" . }}
  labels:
    app: {{ template "qycloud.name" . }}
    chart: {{ template "qycloud.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "qycloud.name" . }}
    release: {{ .Release.Name }}
